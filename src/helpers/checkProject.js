const checkProject = (path) => {
    if (path === '/musicmatch') {
        return 'Music Match'
    } else if (path === '/technoleon') {
        return 'Technoleon'
    }
}

export default checkProject